server:
  port: 8080
  error:
    include-message: always

spring:
  application:
    name: ai-interview-platform
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb+srv://astha:${MONGODB_PASSWORD}@cluster0.sdtsdik.mongodb.net/interviewdb?retryWrites=true&w=majority&appName=Cluster0}
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

application:
  security:
    jwt:
      # 256-bit secret key in hex format
      secret-key: ${JWT_SECRET_KEY:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
      expiration: 86400000 # 24 hours in milliseconds
      refresh-token:
        expiration: 604800000 # 7 days in milliseconds

spring.ai.stt.provider: ${STT_PROVIDER:mock}
spring.ai.provider: ${AI_PROVIDER:mock}
